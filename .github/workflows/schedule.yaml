name: schedule.yaml
on:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  schedule-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello-world
      - name: step2
        run: |
          echo hello-world
          echo github-action
